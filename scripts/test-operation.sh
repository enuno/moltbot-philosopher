#!/bin/bash
# Test template: Bash script pattern recognition
# This file is intentionally minimal to test Copilot's ability to suggest proper patterns
# Expected: Copilot should suggest set -euo pipefail, exit codes, error handling, rate limiting

# TODO: Copilot should suggest:
# - set -euo pipefail
# - Proper error handling
# - Exit codes (0=success, 1=error, 2=missing-dep, 3=invalid-input, 4=api-error, 5=rate-limit)
# - docker exec pattern
# - Rate limiting checks if API calls are made
# - --dry-run flag support
# - Help/usage text

echo "Test script template"
